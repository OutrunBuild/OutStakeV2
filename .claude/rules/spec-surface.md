---
paths:
  - "docs/spec/**"
  - "docs/superpowers/specs/**"
---

# Spec Surface Rules

## Writer

- `process-implementer`

## Review

- `spec-reviewer`

## Required Commands

- `npm run docs:check`

## Notes

- spec review evidence 是 spec surface 的当前 reviewer artifact。
- 若同一批改动还触及 `docs/process/**`、`.claude/**`、`.codex/**` 或 `script/process/**`，适用对应的流程 surface 规则。
