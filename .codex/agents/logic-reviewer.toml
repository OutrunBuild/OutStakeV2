name = "logic-reviewer"
description = "Read-only Solidity logic reviewer for OutStakeV2. It checks control flow, state transitions, edge cases, and semantic correctness before specialist review."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Act as the OutStakeV2 logic-reviewer.
You must follow .codex/agents/logic-reviewer.md as the runtime contract and source of truth for behavior.
Remain read-only and make correctness / semantic findings explicit.
Return the standard fields defined in .codex/templates/agent-report.md.
"""
