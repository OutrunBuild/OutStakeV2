# Task Brief

- Goal: 新增 `OutrunAsBNBSY`，接入 Aster `asBNB`，同步支持 `BNB / slisBNB / asBNB` 入金，仅支持 `asBNB` 赎回，并把 queued mint 建模为硬失败
- Change classification: high-risk
- Change classification rationale: 新增 production `SY` adapter、Aster 外部集成接口与对应测试，命中 `src/yield/**`、`src/integrations/**`、`test/**/*.sol`，且 classifier 当前结果为 `high-risk`，原因是改动集直接触达 `src/yield/**` 与 `src/integrations/**` 的语义敏感表面并改变资金流、外部依赖 quote、canonical asset 计价和 queue 失败策略
- Change type: new production yield adapter with external protocol integration
- Files in scope: src/integrations/aster/interfaces/IAsBnbMinter.sol; src/integrations/aster/interfaces/IYieldProxy.sol; src/integrations/aster/interfaces/IListaBNBStakeManager.sol; src/yield/adapters/aster/OutrunAsBNBSY.sol; test/yield/mocks/AsterSYMocks.sol; test/yield/OutrunAsBNBSY.t.sol; test/yield/OutrunAsBNBSYFuzz.t.sol; test/yield/OutrunAsBNBSYFork.t.sol
- Out of scope: router/position/assets 逻辑改动；Aster queued request 的取消/补领；`tokenOut = slisBNB` 或 `tokenOut = BNB`；异步退出状态机；部署脚本
- Known facts: `SYBase.deposit()` 成功后必须同步得到 `sharesOut`；Aster `mintAsBnb(uint256)` / `mintAsBnb() payable` 活动期可 queue 并返回 `0`；Aster `convertToTokens()` / `convertToAsBnb()` 提供 `asBNB <-> slisBNB` 只读换算；Lista `listaBNBStakeManager` 暴露 `convertBnbToSnBnb(uint256)`；approved spec 已要求构造期缓存 `YIELD_PROXY` / `STAKE_MANAGER` 并做 fail-fast 校验
- Open questions / assumptions: 假设主网 `asBNB` / `slisBNB` / `asBnbMinter` 地址与已核验外部来源一致；fork test 仅做只读 quote/接线验证，不依赖活动窗口下的成功 mint；若 classifier 或 reviewer 将变更升级为 `high-risk`，需补对应 reviewer / security-test-writer 流程
- Risks to check: queued mint revert 是否完全避免半状态；`previewDeposit(BNB)` 是否严格走 `convertBnbToSnBnb -> convertToAsBnb`；constructor 错配校验是否完整；`assetInfo()` 与 repo ERC5115 语义是否一致；fuzz/fork 测试是否足以覆盖外部集成高风险路径
- Required roles: solidity-implementer; logic-reviewer; security-reviewer; gas-reviewer; verifier
- Optional roles: security-test-writer; solidity-explorer
- Verifier profile: full
- Default writer role: solidity-implementer
- Implementation owner: solidity-implementer
- Write permissions: src/integrations/aster/interfaces/IAsBnbMinter.sol; src/integrations/aster/interfaces/IYieldProxy.sol; src/integrations/aster/interfaces/IListaBNBStakeManager.sol; src/yield/adapters/aster/OutrunAsBNBSY.sol; test/yield/mocks/AsterSYMocks.sol; test/yield/OutrunAsBNBSY.t.sol; test/yield/OutrunAsBNBSYFuzz.t.sol; test/yield/OutrunAsBNBSYFork.t.sol
- Writer dispatch backend: native Codex subagent dispatch
- Writer dispatch target: `.codex/agents/solidity-implementer.toml`
- Writer dispatch scope: 严格按 approved spec 与 implementation plan 分 task 实现；先 test/mocks，再 adapter，再 fuzz，再 fork；不得顺手修改 router/position/assets 或引入异步语义
- Non-goals: 不支持 `slisBNB/BNB` 赎回；不实现 Aster queue 处理后台流程；不修改现有 router/position accounting 模型；不补部署脚本
- Acceptance checks: `deposit(asBNB)`/`deposit(slisBNB)`/`deposit(BNB)` happy path 正常；queued mint 返回 `0` 时 `deposit(BNB/slisBNB)` revert `AsBnbMintQueued`；`redeem(asBNB)` 1:1；`previewDeposit(BNB)` 与两跳 quote 一致；`exchangeRate()` 与 native canonical asset 闭环；constructor 对 `asBnb/token/yieldProxy/stakeManager` 错配 fail-fast 且 zero-check 先于 `SYBase` 参数求值；unit + fuzz + fork tests 通过
- Required verifier commands: `npm run codex:review`; `forge fmt --check src/integrations/aster/interfaces/IAsBnbMinter.sol src/integrations/aster/interfaces/IYieldProxy.sol src/integrations/aster/interfaces/IListaBNBStakeManager.sol src/yield/adapters/aster/OutrunAsBNBSY.sol test/yield/mocks/AsterSYMocks.sol test/yield/OutrunAsBNBSY.t.sol test/yield/OutrunAsBNBSYFuzz.t.sol test/yield/OutrunAsBNBSYFork.t.sol`; `forge build`; `forge test -vvv --match-path test/yield/OutrunAsBNBSY.t.sol`; `forge test -vvv --match-path test/yield/OutrunAsBNBSYFuzz.t.sol`; `forge test -vvv --match-path test/yield/OutrunAsBNBSYFork.t.sol`; `npm run quality:gate:fast`; `bash ./script/process/check-solidity-review-note.sh`
- Required artifacts: docs/task-briefs/2026-04-10-outrun-asbnbsy-task-brief.md; docs/agent-reports/2026-04-10-outrun-asbnbsy-solidity-implementer-report.md; docs/agent-reports/2026-04-10-outrun-asbnbsy-logic-reviewer-report.md; docs/agent-reports/2026-04-10-outrun-asbnbsy-security-reviewer-report.md; docs/agent-reports/2026-04-10-outrun-asbnbsy-gas-reviewer-report.md; docs/reviews/2026-04-10-outrun-asbnbsy-review-note.md
- Review note required: yes
- Semantic review dimensions: Aster queue 语义与同步 `deposit()` 对齐；canonical asset / exchangeRate 定义；constructor 接线校验；外部 quote 路径；余额与份额一致性
- Source-of-truth docs: AGENTS.md; docs/process/change-matrix.md; docs/process/review-notes.md; docs/process/policy.json; docs/superpowers/specs/2026-04-10-outrun-asbnbsy-design.md; docs/superpowers/plans/2026-04-10-outrun-asbnbsy.md; src/yield/SYBase.sol; src/yield/interfaces/IStandardizedYield.sol; src/libraries/TokenHelper.sol
- External sources required: Aster mint asBNB docs `https://docs.asterdex.com/earn/overview/mint-asbnb`; Aster smart contracts docs `https://docs.asterdex.com/overview/smart-contracts`; Aster verified implementation at BscScan for `AsBnbMinter`; Lista verified `listaBNBStakeManager` source exposing `convertBnbToSnBnb(uint256)`
- Critical assumptions to prove or reject: Aster queued mint 确实返回 `0` 而非 revert；构造时读取的 `yieldProxy()` / `stakeManager()` 足够定义运行期接线； `exchangeRate = convertToTokens(1e18)` 与 repo asset accounting 兼容； `assetInfo = (TOKEN, NATIVE, 18)` 与现有 native SY 风格一致
- Required output fields: writer Agent Report 必须记录 failing test -> passing test、外部依赖核验、touched files、验证命令结果与 residual risks；reviewer reports 必须覆盖 spec 对齐、控制流/安全/Gas 维度；review note 必须完整串联 artifacts 与 verifier evidence
- Review note impact: Behavior change=yes; ABI change=yes; Storage layout change=no; Config change=yes
- If blocked: 若上游地址/接口行为与已确认外部事实不一致，或 fork 环境缺少稳定只读验证前提，停止实现并回报具体冲突点与受影响 task
