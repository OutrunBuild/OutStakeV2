# Task Briefs

本目录存放 OutStakeV2 本地 `Task Brief` 工件。

- `Task Brief` 是主会话在派发 writer / reviewer / verifier 前生成的结构化任务边界
- `Task Brief` 不属于 design / implementation plan，因此不应放入 `docs/plans/`
- `docs/agent-reports/` 只保留 `Agent Report`

命名建议：

- `<date>-<topic>-task-brief.md`
